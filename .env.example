# PostgreSQL 연결 문자열
# Neon(https://neon.tech) 무료 티어에서 프로젝트를 만들면 이 형태의 주소를 줍니다.
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# 세션 쿠키 서명 키 — 32자 이상. 아래 명령으로 만들어 붙여 넣으세요.
#   openssl rand -base64 32
# 이 값이 바뀌면 모두 로그아웃됩니다. 절대 저장소에 커밋하지 마세요.
AUTH_SECRET=""
